# --- training + evaluation (notebooks 02-04) ---
jax>=0.4.30            # add [cuda12] for GPU, see main README
equinox>=0.11
optax>=0.2
numpy
h5py
matplotlib
imageio
tqdm
# imageio-ffmpeg        # optional, for mp4 export in notebook 04

# --- data generation (notebook 01; typically a separate environment) ---
# ehtim==1.2.10
# astropy
# scikit-image
