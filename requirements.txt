# Tutorial dependencies. The core library installs via `pip install -e .` (see pyproject.toml);
# this file adds the tutorial-only extras.

# core (training / evaluation)
jax>=0.4.30
equinox
optax
numpy
h5py
imageio
matplotlib
tqdm

# pixel tutorial (weather data)
xarray
netCDF4
Cartopy

# Fourier tutorial data generation (notebook 01 only; often a separate env)
ehtim==1.2.10
astropy
scikit-image
